type Query {
    bookById(id:String!): Book
}

type Book {
    id: String
    name: String
    pageCount: Int
    pageCount2: Int
}

type Author {
    id: String
    firstName: String
    lastName: String
}